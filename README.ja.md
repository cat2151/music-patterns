# cat-music-patterns

## 用途
- cmrt-client-playgroundから利用
  - chord進行
  - Tone.jsモード（cmrtをlocalで起動していないとき）のときの、Tone.js音色
- cmrtから利用
  - `surge-xt-patch-voicing.json` : keyboard画面でのchord/アルペジオ の判別用（自動判別結果）
  - `surge-xt-patch-voicing-overrides.json` : 上に同じ（手動補正結果）

## 頻繁に破壊的変更をします
